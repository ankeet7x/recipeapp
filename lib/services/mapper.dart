class Recipes {
  String title;
  String imgUrl;

  Recipes({this.imgUrl, this.title});

  // factory Recipes.fromJson(Map<String, dynamic> json) {
  //   return Recipes(title: json['title'], imgUrl: json['image']);
  // }
}
